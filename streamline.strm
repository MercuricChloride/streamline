interface Erc721 {
    event Transfer(address from, address to, uint256 tokenId);
    event Approval(address from, address to, uint256 tokenId);
}

Erc721 bayc = Erc721(0xbc4ca0eda7647a8ab7c2061c2e118a18a936f13d);

struct SomethingElse {
     address from;
     address to;
     uint tokenId;
}

map transfers:
(bayc.Transfer[]) -> SomethingElse[] {
    map (transfer) => SomethingElse {
        from: transfer.from,
        to: transfer.to,
        tokenId: transfer.tokenId
    };
}
