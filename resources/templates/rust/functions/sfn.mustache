#[substreams::handlers::store]
pub fn {{name}}({{inputs}}, s: {{output}}) {
    let store = |k, v| s.store(k, v);
    let module_output_value = ({{input-names}});

    {{{body}}}

    module_output_value.into()
}
